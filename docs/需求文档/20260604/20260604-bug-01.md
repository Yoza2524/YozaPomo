## 当日bug记录

#### 阶段六后验证

1. 一轮验证
   1. [x] 没有TODO时应该可以开始专注 — 已修复：移除 `FloatingWindow.vue` 中 `FocusButton` 的 `disabled` 条件
   2. [x] 在后台中增加TODO，右上角无反应 — 已修复：Rust `todo_commands` 中增/改/删操作后 emit `todo-changed` 事件，悬浮窗 `listen` 后刷新
   3. [x] 点击开始专注无反应 — 同 Bug 1，根因是 `disabled=true` 时 `handleClick` 直接 return
2. 二轮验证
   1. [x] 点击开始专注依旧没有反应 — 已修复：Rust 所有 struct 添加 `#[serde(rename_all = "camelCase")]`，前端 camelCase 与 Rust snake_case 不匹配导致 `invoke` 反序列化静默失败；同时 `FocusButton` 增加错误提示
   2. [x] todo标记结束后右上角没有消失 — 已修复：`todoStore.fetchTodayTodos()` 过滤 `completed === 0`，已完成的 TODO 不再显示在悬浮窗
3. 三轮验证
   1. [x] 开始专注后番茄钟模块不影响TODO列表 — 已修复：`FocusButton` 重构为水平排布 [结束专注] [倒计时]，`FocusPanel` 移除，TODO 列表始终可见
4. 四轮验证
   1. [x] 在后台界面被关闭后，左键显示/隐藏失效，且无法被呼出
   2. [x] 最小专注时间允许设置为1分钟
   3. [x] TODO列表中点击详情弹出界面设置为不透明
   4. [x] 今日TODO，改名为TODOs，并使用和TODO列表同样的详情页
5. 五轮验证
   1. [x] 开始休息是结束专注改名，本质是一样的
   2. [x] 不要在下面显示已专注总时长，在显示超出时长的位置直接显示”总时长 xx:xx”
   3. [x] 光标在悬浮窗边缘时，鼠标会变为拖动状态
   4. [x] 依旧不能在没有按钮的位置穿透点击到下方内容 — 已修复：用 useAutoResize 替代 useClickThrough，窗口自动收缩到有内容的区域，透明区域自然不存在
   5. [x] 透明区域右键依旧会显示当前软件右键菜单 — 已修复：FloatingWindow 根容器添加 @contextmenu.prevent 阻止右键菜单

6. 六轮验证
   1. [x] 管理界面窗口变得超级矮（900×136） — 已修复：App.vue 中 windowLabel 默认值为 'floating'，onMounted 才检测窗口标签，导致 FloatingWindow 的 useAutoResize 在管理窗口中误执行将其缩小。改为 setup 阶段同步检测窗口标签
