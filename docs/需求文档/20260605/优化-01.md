# 优化内容记录

### 晚间场

- [x] 优化 **专注检测提醒** 的逻辑：

  - **专注状态**下 每5min检测一次，可设置时长。

  - **超时专注状态**下 每3min检测一次，可设置时长。

  - **专注检测提醒** 的方式修改
    - 修改前： 弹出“还在吗？”按钮并开始监听输入持续一分钟，一分钟后按钮消失专注异常结束。
    - 修改后： 
      - 按钮在前2s内不出现且监听输入，2s内未监听的输入，按钮出现。
      - 按钮监听到消失的异常改为 设置 **异常检测持续时长**，范围为3-90s。
  - [x] 遗留问题：
    - [x] 提示音应为和烟花同时出现，所以在专注状态下的检测“还在吗”应该没有声音
    - [x] 超时专注状态下，烟花为指定时间出现，然后2s内无操作再提示“还在吗”

- [x] 设置页优化
  - [x] 增加设置项 “专注检测间隔” “超时专注检测间隔” “检测滞留时长”
  - [x] 修改专注时长和休息时长为xx分 xx秒，如果分钟数被改为0，则秒数至少为5；秒数被改为0，分钟数最少为1。
  - [x] 设置页需要铺满整个窗口

  - [x] 设置页中button input select控件改为同样高度，增大圆角，我想要类ios风格的设置页
  - [x] 移除保存设置按钮，失去焦点自动保存
  - [x] input可以用回车来保存并失焦，esc取消修改并失焦
  - [x] input中的内容被完全删除后不显示任何默认内容
  
- [x] Tab line优化
  - [x] 删除下横线
  - [x] 美观优化

- [x] logo优化

- [ ] 悬浮窗界面优化

  - [x] 今日TODO中最先创建的TODO要显示在上面

  - [x] TODO超过显示上限的时候，只显示省略号，不要影响和下方按钮的间距

  - [x] 暂无待办时下方提示“在管理界面添加TODO” 修改为“双击添加TODO”，双击唤起主界面，到对应tab

  - [x] 修改悬浮窗为可移动的窗口，同时在设置中设置是否钉住。

  - [ ] 开始专注按钮和TODO tag的长度不一样

  - [ ] 有背景后TODO tag和背景颜色过于接近

  - [ ] 悬浮窗增加动效

    - [x] 点击开始专注时（结束时相反）

      开始专注按钮变为结束专注按钮并向左变窄，右侧逐渐出现计时块
  
    - [x] 创建任务时
  
      在主界面创建完任务，不再像当前闪烁刷新
  
      - [x] 如果当前悬浮窗中没有任务，暂无待办等文字消失，新建的TODO tag从中间向两边伸展出现
      - [x] 如果TODO list 已达上限，则出现一个分裂出的tag向下并缩小变为省略号
    
    - [x] TODO的专注
    
      - [x] 双击开始TODO后，TODO的
    
    - [ ] 完成任务
    
    - [ ] 删除任务
    
      - [ ] 删除列表中的TODO时，且无省略号，出现粒子特效，碎片坠落，tag逐渐消失，下方内容上移窗口大小逐渐变化
      - [ ] 删除列表中的TODO时，且有省略号，出现粒子特效，碎片坠落，从省略号中向上出现逐渐变大的小tag，填充tag位置。如果省略号中移出最后一个tag，则省略号缓慢消失，下方内容上移窗口大小逐渐变化
      - [ ] 删除列表中最后一个Tag时，出现粒子特效，碎片坠落，tag缓慢消失，“暂无待办” 缓慢进入，保持二者速度相同以保证外部窗口大小无变化(把暂无待办块修改为和一个Tag等高)



























